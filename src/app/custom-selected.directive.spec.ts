import { CustomSelectedDirective } from './custom-selected.directive';

describe('CustomSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
